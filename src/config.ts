#!/usr/bin/env node
'use strict';

export default class Config {
	public static init(base: string, folder: string) {

	}

	private constructor(base: string, folder: string, print: boolean = false) {
	}
}
